# Discoverability gate

Status before private repository creation: `READY`; final GitHub metadata verification remains pending.

## Reviewed artifacts

- `README.md`: the title and first paragraph state the coding-agent byte-preservation problem, target users, and natural file-integrity, CRLF, BOM, encoding, Unicode, and executable-mode terms.
- `README.md`: quickstart, guarded adapter usage, stale-write workflow, limitations, and comparison sections match practical search intent without adoption claims.
- `pyproject.toml`: package name, description, keywords, supported Python versions, and testing category are accurate.
- `BENCHMARK.md`: exact baseline, Codex/opencode comparison, reproducible failures, and workflow improvement are documented.
- `DISTRIBUTION.md`: GitHub, package-search, issue-evidence, and technical benchmark channels are concrete.
- `RELEASE_NOTES.md`: release title/content describe the six fixture classes, adapters, and limitations without hype.

## Prepared GitHub metadata

Description: `Exact byte-preservation tests for coding-agent edits across CRLF, BOM, legacy encodings, Unicode, final newlines, and file modes.`

Topics: `coding-agents`, `agent-testing`, `file-integrity`, `encoding`, `line-endings`, `crlf`, `developer-tools`, `python`.

The gate becomes `PASS` only after the private repository description/topics match these values and the committed artifacts above are visible on the remote default branch.
